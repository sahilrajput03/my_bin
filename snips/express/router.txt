// PRO TIP:: Don't forget to call next() if you have multiple requests of same type (get/post/etc) for same path (say "/trades").
// PRO TIP:: Always use `return` with while send/end`ing a request, ``return res.send('my text...')`` or ``return res.status(505).end()``
//##file: ``birds.js``
var express = require('express')
var router = express.Router()

//middleware that is specific to this router
router.user(...)

router.get('/', ...)

module.exports = router


//##file ``server.js``
var birds = require('./birds')

// ...

app.use('/birds', birds)
